---
title: "Quickstart"
description: "Run OpenDiff locally"
---

# Quickstart

Install dependencies from the repository root:

```bash
bun install
```

Run the app package:

```bash
bun run --cwd packages/app dev
```

Run the website package:

```bash
bun run --cwd packages/website dev
```
