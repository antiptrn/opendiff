---
title: "Quickstart"
description: "Run OpenDiff locally"
---

# Quickstart

Install dependencies from the repository root:

```bash
bun install
```

Run the app package:

```bash
bun run --cwd apps/app dev
```

Run the site package:

```bash
bun run --cwd apps/site dev
```
