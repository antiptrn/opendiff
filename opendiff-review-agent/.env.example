# GitHub Webhook Secret
# Generate with: openssl rand -hex 32
GITHUB_WEBHOOK_SECRET=your-webhook-secret-here

# === GitHub Authentication (choose one method) ===

# Option 1: GitHub App (recommended)
# Get App ID from: https://github.com/settings/apps/YOUR-APP
GITHUB_APP_ID=
# Path to the private key .pem file downloaded from the App settings
GITHUB_PRIVATE_KEY_PATH=/app/private-key.pem

# Option 2: Personal Access Token (with repo scope)
# Create at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Anthropic API Key
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxx

# Bot configuration
BOT_USERNAME=opendiff-bot
# Comma-separated list of teams the bot belongs to (for team review requests)
BOT_TEAMS=

# Server port (default: 3000)
PORT=3000

# Settings API URL (optional)
# URL to the opendiff-bff for fetching repository settings
# If not set, all repos are enabled with triage mode on by default
SETTINGS_API_URL=http://localhost:3001

# Review Agent API Key
# Shared secret for authenticating with the server when recording reviews
# Must match the REVIEW_AGENT_API_KEY set on the server
REVIEW_AGENT_API_KEY=your_review_agent_api_key
